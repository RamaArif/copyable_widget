name: copyable_widget
description: >-
  Zero-boilerplate clipboard copy for any Flutter widget or text,
  with haptic feedback and SnackBar confirmation.
version: 1.3.0
homepage: https://github.com/RamaArif/copyable_widget
repository: https://github.com/RamaArif/copyable_widget
issue_tracker: https://github.com/RamaArif/copyable_widget/issues

topics:
  - clipboard
  - clipboard-widget
  - copy
  - copyable
  - copyable-widget

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
